
// import React, {Children} from 'react'
// import {number} from 'prop-types'

// export function Take(props) {
//   const take = Math.min(Children.count(props.children), props.count)
//   return Children.toArray(props.children).slice(0, Math.max(0, take))
// }

// Take.defaultProps = {
//   count: 1
// }

// Take.propTypes = {
//   count: number
// }
